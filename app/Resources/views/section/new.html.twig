{% extends 'base.html.twig' %}

{% block body %}
    <h1>Section création</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class='btn btn-primary' type="submit" value="Créer" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('admin_section_index') }}">Retour à la liste d'articles</a>
        </li>
    </ul>
{% endblock %}
